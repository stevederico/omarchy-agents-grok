#!/bin/bash
# Keep the Grok usage collector timer running after login.
systemctl --user enable --now omarchy-agent-usage-grok.timer omarchy-agent-usage-grok.path >/dev/null 2>&1 || true
systemctl --user start omarchy-agent-usage-grok.service >/dev/null 2>&1 || true
